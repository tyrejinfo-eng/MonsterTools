.

This is the bit to put into instructions file:

# IMPORTANT - Credit Efficiency - MANDATORY

GitHub Copilot now operates under strict AI credit limits. AI credits are limited and must be conserved. Do not consume large context unless it is necessary to complete the task correctly.

Rules:
- Be concise by default.
- Use the narrowest context that can still produce a correct answer.
- Do not re-read instructions, logs, status files, or repo-wide context unless the current task genuinely depends on them.
- Do not refresh working state unless switching topic/feed, writing new SQL, updating tracked status, or resolving conflicting evidence.
- Prefer one complete response over multiple incremental follow-ups.
- Avoid unnecessary prose, repeated summaries, and duplicate checks.
- Correctness comes first, but unnecessary token usage is a failure.
